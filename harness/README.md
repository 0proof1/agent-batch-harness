# Harness

The Python package in `src/shardflow` is the canonical harness implementation.
This directory is reserved for thin wrappers, shell integrations, and deployment
helpers that call the `shardflow` CLI.

