# Harness

The Python package in `src/agent_batch_harness` is the canonical harness implementation.
This directory is reserved for thin wrappers, shell integrations, and deployment
helpers that call the `agent-batch` CLI.
